# Copy to .env.local and fill in the two public values from your Supabase
# project (Project Settings -> API). Both are safe to ship to the browser.
# Never put the service_role key, the database password or any secret here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
