node_modules
dist
.DS_Store
*.log

# Raw interview transcripts are kept out of the public repository.
# Participants consented to a university project, not to publication.
# The findings drawn from them are summarised in README.md instead.
*.txt

# Course material belongs to the unit, not in a public repo.
*.pdf

# Supabase credentials live in local env files only. The example file with
# empty placeholders is the one that ships.
.env
.env.*
!.env.example

# Supabase CLI local state, if the CLI is ever used here.
supabase/.temp
supabase/.branches
